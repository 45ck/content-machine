# Micro-Doc Breakdown

Status: `showcase candidate`

Topic: `how Blockbuster actually died`

Final render:

- `outputs/final/video.mp4`
- tracked preview:
  `docs/demo/demo-17-micro-doc-breakdown.mp4`

## Review

The latest render passes the base publish-prep path without OCR caption
sync:

- resolution: `1080x1920`
- duration: `41.0s`
- format: `mp4/h264/aac`
- cadence: median cut interval `2.43s`
- audio-signal: `-18.4 LUFS`, peak `-1.1 dBFS`
- script score: passed

Full OCR caption-sync still fails:

- `21/21` expected caption segments matched
- median start drift: `617ms`, above the `350ms` gate
- P95 start drift: `2380ms`, above the `900ms` gate

This should stay a proving candidate until the caption sync verifier and
rendered caption timing agree on the long-tail drift, and until the
evidence-card inserts are replaced with a stronger documentary visual
language.

## Production Notes

- The first assembly failed cadence because long card scenes held too
  long.
- Subtle zoom/crop changes were not enough for cadence detection.
- The polished render is generated by
  `tools/build-polished-microdoc.mjs`.
- The passing base render uses a smaller dark caption dock and
  documentary-style evidence inserts to satisfy cadence and readability,
  but this is not the final aesthetic bar for the lane.
- FFmpeg script calls must use `-nostdin`; otherwise FFmpeg can consume
  later shell lines as interactive commands.
