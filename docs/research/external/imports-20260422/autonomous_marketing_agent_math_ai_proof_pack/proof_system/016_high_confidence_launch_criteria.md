# High Confidence Launch Criteria

A campaign can launch when:

```text
buyer state named
hypothesis stated
tactic selected
offer clear
proof path built
metrics defined
budget envelope set
pause rules set
learning memo template ready
```

If any are missing, the agent generates the missing artifact before launch.
